{
  "id": "github",
  "name": "GitHub",
  "type": "mcp",
  "description": "Read and manage repositories, issues and pull requests. This is the archived stdio server, kept because it is the only one of the two that works in OpenCode — prefer the official GitHub plugin on Claude Code and Codex.",
  "source": "GitHub — npm @modelcontextprotocol/server-github (deprecated upstream)",
  "install": {
    "command": "npx",
    "args": [
      "-y",
      "@modelcontextprotocol/server-github@2025.4.8"
    ]
  },
  "secrets": [
    {
      "key": "GITHUB_PERSONAL_ACCESS_TOKEN",
      "label": "GitHub personal access token",
      "help": "repo scope is enough for the demo"
    }
  ],
  "supportedAgents": [
    "claude",
    "codex",
    "opencode"
  ]
}
